# Normalize line endings; let git handle conversion
* text=auto

# Scripts that must keep specific endings
*.sh    text eol=lf
*.ps1   text eol=crlf
*.cmd   text eol=crlf

# Binaries
*.ico   binary
*.png   binary
*.jpg   binary
*.woff2 binary
